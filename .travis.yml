language: csharp

matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      dotnet: 1.0.1
    - os: osx
      osx_image: xcode8
      dotnet: 1.0.1

script:
  - git fetch --unshallow --tags
  - dotnet --info
  - export FrameworkPathOverride=/usr/lib/mono/4.5/
  - ./build.sh

name: Post productivity note
on:
  pull_request:
    branches: [ main ]
    types: [ opened, closed, reopened, synchronize ]
jobs:
  devrating:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0  # important
    - uses: victorx64/devrating-gh-action@0
